<%''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''Function DeleteRecord(tableName, tableID)		Dim objDBs	Dim tempSQL		If Not IsNumeric(tableID)  Then		tableID = 0		'End Function	End If		Set objDBs = Server.CreateObject("ADODB.Connection")	objDBs.Open DATABASE	tempSQL = "Delete from " & tableName & " where " & tableName & "id = " & tableID	'Response.Write(tempSQL)		objDBs.Execute(tempSQL) '"select name from venue where venueid = 1 ")		'objRSs.Open tempSQL objDBs				'Do While Not objRSs.EOF	'      GetName = objRSs(fieldName)	'      objRSs.MoveNext	'Loop		objDBs.Close	Set objDBs = Nothing	End Function 'DeleteRecord()%>